<UserControl x:Class="InwardDC.App.Views.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="12">
        <Border DockPanel.Dock="Top" Background="White" BorderBrush="#D5E0EC" BorderThickness="1" Padding="10" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="Create Backup" Command="{Binding CreateCommand}" Style="{StaticResource PrimaryButton}" />
                <Button Content="Restore from File..." Command="{Binding RestoreCommand}" Style="{StaticResource ToolbarButton}" />
                <Separator Width="1" Height="24" Background="#CCCCCC" Margin="8,0" />
                <Button Content="Factory Reset..." Command="{Binding FactoryResetCommand}" Style="{StaticResource ToolbarButton}"
                        Foreground="#B00020" />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#EEF3F9" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ErrorMessage}" VerticalAlignment="Center" Foreground="#B00020" Margin="12,0,0,0" />
            </StackPanel>
        </Border>

        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="Available Backups" FontWeight="Bold" FontSize="14" Foreground="#1F4E78" Margin="0,0,0,8" />
            <DataGrid ItemsSource="{Binding Backups}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      HeadersVisibility="Column" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Backup File" Binding="{Binding}" Width="*" />
                </DataGrid.Columns>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Restore this backup"
                                  Command="{Binding PlacementTarget.DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding}" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </DockPanel>
    </DockPanel>
</UserControl>
