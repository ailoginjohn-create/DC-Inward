<UserControl x:Class="InwardDC.App.Views.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="12">
        <Border DockPanel.Dock="Top" Background="White" BorderBrush="#D5E0EC" BorderThickness="1" Padding="10" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="Export" Command="{Binding ExportCommand}" Style="{StaticResource ToolbarButton}" />
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="16,0,0,0" />
                <TextBox Width="180" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="6,0" />
                <TextBlock Text="Action:" VerticalAlignment="Center" />
                <ComboBox Width="130" ItemsSource="{Binding Actions}" SelectedItem="{Binding Action}" Margin="4,0" />
                <TextBlock Text="From:" VerticalAlignment="Center" />
                <DatePicker Width="110" SelectedDate="{Binding FromDate}" Margin="4,0" />
                <TextBlock Text="To:" VerticalAlignment="Center" />
                <DatePicker Width="110" SelectedDate="{Binding ToDate}" Margin="4,0" />
                <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource ToolbarButton}" />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#EEF3F9" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding TotalCount, StringFormat={}{0} record(s)}" VerticalAlignment="Center" />
                <Button Content="Prev" Command="{Binding PrevPageCommand}" IsEnabled="{Binding CanGoBack}" Style="{StaticResource ToolbarButton}" />
                <TextBlock Text="Page" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Page, StringFormat={}{0}}" VerticalAlignment="Center" />
                <Button Content="Next" Command="{Binding NextPageCommand}" IsEnabled="{Binding CanGoNext}" Style="{StaticResource ToolbarButton}" />
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="16,0,0,0" />
            </StackPanel>
        </Border>

        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="When" Binding="{Binding Timestamp, StringFormat=g}" Width="140" />
                <DataGridTextColumn Header="User" Binding="{Binding FullName}" Width="140" />
                <DataGridTextColumn Header="Action" Binding="{Binding ActionName}" Width="90" />
                <DataGridTextColumn Header="Entity" Binding="{Binding EntityType}" Width="100" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
