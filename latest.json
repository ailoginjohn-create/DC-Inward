{
  "version": "1.0.10",
  "url": "https://github.com/ailoginjohn-create/DC-Inward/releases/download/v1.0.10/InwardDC.exe",
  "notes": "Import performance and reliability fixes. Inward and DC imports are now much faster on large files (batched serial checks, chunked saves), show live progress while importing, and pop up an error dialog instead of silently failing. Both imports also accept a file with some bad rows and import the valid ones, reporting the skipped rows."
}
